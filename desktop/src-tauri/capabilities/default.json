{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for Safeparts commands and local UI features.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
