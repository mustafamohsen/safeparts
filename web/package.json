{
  "name": "ssss-mnemo-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "bash -lc 'command -v wasm-pack >/dev/null 2>&1 || cargo install wasm-pack --locked --version 0.13.1; command -v wasm-bindgen >/dev/null 2>&1 || cargo install wasm-bindgen-cli --version 0.2.108 --locked; wasm-pack build ../crates/ssss_mnemo_wasm --mode no-install --target web --out-dir ../../web/src/wasm_pkg'",

    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.0"
  }
}
