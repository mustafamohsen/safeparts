---
title: Quickstart
description: Run a safe practice split and learn the minimum recovery model.
---

import { Card, CardGrid, LinkButton, Steps } from '@astrojs/starlight/components';

Start with a practice run. A short drill teaches the workflow without exposing a real secret.

:::caution
Practice with a synthetic secret such as <span class="token">synthetic-secret</span>. Do not use a production key until you have tested recovery.
:::

## The recovery model

You choose two numbers:

- <var>n</var>: how many shares Safeparts creates.
- <var>k</var>: how many shares are required to recover the secret.

With fewer than <var>k</var> shares, recovery should not reveal useful information. With any <var>k</var> shares from the same split, recovery succeeds.

The core supports <span class="token" dir="ltr">1 &lt;= k &lt;= n &lt;= 255</span>. For real recovery plans, use <span class="token">k >= 2</span> so no one holder can recover alone.

## Run the drill

The web app is the fastest way to learn the flow.

<LinkButton href="/" icon="external" target="_blank" rel="noopener noreferrer">
	Open the web app
</LinkButton>

<Steps>
1. Open the <span class="token">Split</span> tab.
2. Enter <span class="token">synthetic-secret</span> as the secret.
3. Set <span class="token">k=2</span> and <span class="token">n=3</span>.
4. Choose <span class="token">Words</span> if you want to read or print the shares, or <span class="token">Letters</span> if you want compact text.
5. Click <span class="token">Split</span> and copy the three shares into temporary notes.
6. Open the <span class="token">Combine</span> tab and recover the secret with any two shares.
7. Delete the synthetic shares and notes.
</Steps>

## Pick the right interface

<CardGrid>
	<Card title="Web app" icon="laptop">
		Best for a quick, local browser workflow. The browser UI exposes <span class="token">base64url</span> and <span class="token">mnemo-words</span>.
		
		[Use the web app](../web-ui/)
	</Card>
	<Card title="Desktop app" icon="laptop">
		Best when you want an installed local app built with Tauri.
		
		[Use desktop](../desktop/)
	</Card>
	<Card title="CLI" icon="document">
		Best for repeatable runbooks, files, and automation.
		
		[Use the CLI](../cli/)
	</Card>
	<Card title="TUI" icon="setting">
		Best for guided terminal workflows, especially on offline machines.
		
		[Use the TUI](../tui/)
	</Card>
</CardGrid>

## Before using a real secret

1. Choose a recovery plan you can run under stress.
2. Store shares in separate failure domains: different people, accounts, devices, or locations.
3. Write down who holds each share and how recovery is approved.
4. Run one recovery drill with synthetic data.
5. Read the [security model](../security/) before handling production material.

Next: [choose a recovery plan](../use-cases/).
